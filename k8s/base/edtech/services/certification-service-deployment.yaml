apiVersion: apps/v1
kind: Deployment
metadata:
  name: certification-service
  namespace: edtech
  labels:
    app: certification-service
    version: v1
    component: service
    part-of: edtech
spec:
  replicas: 2
  selector:
    matchLabels:
      app: certification-service
  template:
    metadata:
      labels:
        app: certification-service
        version: v1
        component: service
        part-of: edtech
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8009"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: certification-service
          image: edtech/certification-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8009
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: CERTIFICATION_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: CERTIFICATION_DB_PASSWORD
            - name: DATABASE_URL
              value: "postgresql://certification_service:$(CERTIFICATION_DB_PASSWORD)@postgresql.edtech.svc.cluster.local:5432/certification_service"
            - name: USER_SERVICE_URL
              value: http://user-service.edtech.svc.cluster.local:8001
            - name: COURSE_SERVICE_URL
              value: http://course-service.edtech.svc.cluster.local:8002
            - name: NOTIFICATION_SERVICE_URL
              value: http://notification-service.edtech.svc.cluster.local:8005
            - name: REDIS_ADDR
              value: redis.edtech.svc.cluster.local:6379
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REDIS_PASSWORD
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: otel-collector.monitoring.svc.cluster.local:4317
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: certification-service
  namespace: edtech
  labels:
    app: certification-service
    version: v1
    component: service
    part-of: edtech
spec:
  type: ClusterIP
  selector:
    app: certification-service
  ports:
    - name: http
      port: 8009
      targetPort: http
