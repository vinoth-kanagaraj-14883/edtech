apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: edtech
spec:
  ingressClassName: nginx
  rules:
    - host: edtech.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 8080
