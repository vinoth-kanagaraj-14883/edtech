{{- if .Values.apiGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    {{- include "edtech.serviceLabels" (dict "context" . "name" "api-gateway") | nindent 4 }}
spec:
  replicas: {{ .Values.apiGateway.replicas }}
  selector:
    matchLabels:
      {{- include "edtech.selectorLabels" (dict "Release" .Release "name" "api-gateway") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "edtech.selectorLabels" (dict "Release" .Release "name" "api-gateway") | nindent 8 }}
    spec:
      containers:
        - name: api-gateway
          image: "{{ .Values.apiGateway.image.repository }}:{{ .Values.apiGateway.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.apiGateway.containerPort }}
          env:
            {{- include "edtech.renderEnv" .Values.apiGateway.env | nindent 20 }}
          resources:
            {{- toYaml .Values.apiGateway.resources | nindent 20 }}
          livenessProbe:
            httpGet:
              path: {{ .Values.apiGateway.probes.liveness.path }}
              port: {{ .Values.apiGateway.containerPort }}
          readinessProbe:
            httpGet:
              path: {{ .Values.apiGateway.probes.readiness.path }}
              port: {{ .Values.apiGateway.containerPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    {{- include "edtech.serviceLabels" (dict "context" . "name" "api-gateway") | nindent 4 }}
spec:
  type: {{ .Values.apiGateway.service.type }}
  selector:
    {{- include "edtech.selectorLabels" (dict "Release" .Release "name" "api-gateway") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.apiGateway.service.port }}
      targetPort: {{ .Values.apiGateway.containerPort }}
{{- end }}
