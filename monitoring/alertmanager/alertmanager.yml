global:
  resolve_timeout: 5m

route:
  receiver: dev-webhook
  group_by: [alertname, job, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: dev-webhook
    webhook_configs:
      - url: http://webhook-receiver:5001/alerts
        send_resolved: true
