---
- name: Restart PostgreSQL
  ansible.builtin.systemd:
    name: postgresql
    state: restarted
    enabled: true
    daemon_reload: true

- name: Restart postgres exporter
  ansible.builtin.systemd:
    name: postgres-exporter
    state: restarted
    enabled: true
    daemon_reload: true
