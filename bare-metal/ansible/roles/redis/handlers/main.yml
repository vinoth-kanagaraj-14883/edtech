---
- name: Restart Redis
  ansible.builtin.systemd:
    name: redis
    state: restarted
    enabled: true
    daemon_reload: true

- name: Restart redis exporter
  ansible.builtin.systemd:
    name: redis-exporter
    state: restarted
    enabled: true
    daemon_reload: true
