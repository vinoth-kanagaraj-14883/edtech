---
- name: Restart MySQL
  ansible.builtin.systemd:
    name: mysql
    state: restarted
    enabled: true
    daemon_reload: true

- name: Restart mysqld exporter
  ansible.builtin.systemd:
    name: mysqld-exporter
    state: restarted
    enabled: true
    daemon_reload: true
