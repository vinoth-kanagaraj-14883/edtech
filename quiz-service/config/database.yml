default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch('DB_POOL', 5) %>
  url: "<%= ENV.fetch('DATABASE_URL') %>"

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
